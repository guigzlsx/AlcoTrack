{
  "rules": {
    ".read": false,
    ".write": false,
    "events": {
      "$eventId": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['id', 'name', 'code', 'createdAt'])"
      }
    }
  }
}
